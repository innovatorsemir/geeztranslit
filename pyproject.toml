[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fidel"
version = "0.1.0"
description = "Geez to Latin transliteration package"
requires-python = ">=3.8"

[tool.hatch.build.targets.wheel]
packages = ["fidel"]